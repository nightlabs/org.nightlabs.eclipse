package org.nightlabs.base.ui.editor;

public class UndirtyBehaviour {
	/**
	 * The possibility to mark form parts / section parts / editors
	 * as undirty did not work reliable and was commented.
	 * Now there is a fix.
	 * To disable this behaviour in case it doesn't work well,
	 * set this boolean to false.
	 */
	public static final boolean ENABLED = true;
}
